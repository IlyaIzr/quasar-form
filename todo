=-=-=-=-=_ Todo _=-=-=-=-=

mask extention
qdecimal or https://codepen.io/pdanpdan/pen/KKKrwbe?editors=1010


check prop names unity in fields

pure calendar validation

const mask = createMask(config)

mask.resolve('value')

date with input clearable

check options for multiple or remake setOptions to do setConfig

close buttons for tabs

-=-= Agenda =-=-
multiple item border, bigger label, and centered and capitalize, close button over border
opt out close title
wrapper around some rows
distance between required and unrequired fields
different clear  button (smaller)
vnode.closeModal()
.validate() without $refs.validate()
getMultiSiblings(el) => el.$children[0]


=-=-=-=-=_ Done list _=-=-=-=-=

25.11.2020
* default required fields and rules fix
* form reset() and clear() methods, form.onReset() and form.onClear() listeners in config. Clear button type added
* autocomplete perfomance improvment
* autocomplete with multiple options solved
* tabs fixes and clear button
* multifield fixes

26.11.2020
* script for autobuild
* '*' stack when tabs submit Error occurs
?* show validation error when pushed back in tabs
* visible classes improvment

27.11.2020
* vNodeStore for multiple structure
* tab refocus fix

30.11.2020
* configStore and setConfig()
* multiple base values

01.12.2020
* reveal password interactive icon
* appended and prepend input icons and functions
* visibility fix

02.12.2020
* multiple validation check
* multiple reset () fix
* form tabs fixed headers and control buttons
